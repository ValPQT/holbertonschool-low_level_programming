#include "main.h"
#include <stdio.h>
#include <stdlib.h>

char *str_concat(char *s1, char *s2)
{
	int i, j;

	for (i = 0; *s1 != '\0' ; i++)
	{
		s1++;
	}
	return ('i');

	for (j = 0; *s2 != '\0' ; i++)
	{
		s2++;
	}
	return ('j');
}
