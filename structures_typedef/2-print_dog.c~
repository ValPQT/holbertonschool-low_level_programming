#include <stdio.h>
#include "dog.h"

void print_dog(struct dog *d)
{
	if (d == NULL)
		return;

	if (d->name = NULL)
		printf ("Name: (nil)");
	if (d->owner = NULL)
		printf ("Owner: (nil)");

	return 0;
}
