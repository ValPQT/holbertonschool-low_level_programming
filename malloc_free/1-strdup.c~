#include "main.h"
#include <stdio.h>
#include <stdlib.h>

char *_strdup(char *str)
{
	char *dup;
	unsigned int i, len;

	i = 0;
	len = 0;

	if (str == NULL)
		return (NULL);

	for (i = 0; *str != '\0'; i++)
	{
		s++;
	}

	str = malloc(sizeof(int) * i);

		if (str == NULL)
                        return (NULL);

		while (str[i] != '\0' && i < str)
		{
			dup[i] = str[i];
			i++;
		}

		while (i < n)
		{
			dup[i] = '\0';
			i++;
		}

		return (dup);

	        }
