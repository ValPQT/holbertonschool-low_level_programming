#include <stdio.h>
#include "lists.h"

size_t print_list(const list_t *h)
{
	size_t len;
	len = 0;

	while (h)
	{
		if (h->str == NULL)
			printf("[0](nil)\n");
		else (h->str != NULL)
			     printf("[%u] %s\n", h->len, h->str);
		h = h->next;
		len++;
	}
	return(len);
}
