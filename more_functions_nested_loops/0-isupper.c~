#include "main.h"
/**
 * _islower - check if the given character is upper case.
 * @c: character to be tested
 * Return: 1 (True) else 0 (False)
 *
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	return (0);
}
