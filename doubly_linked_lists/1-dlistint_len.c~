#include <stdio.h>
#include "lists.h"

size_t dlistint_len(const dlistint_t *h)
{
	size_t nodes = 0;

	if (h == NULL)
		return (NULL);

	while (h->prev != NULL)
	{
		h = h->prev;
	}
	while (h != NULL)
	{
		nodes++;
		h = h->next;
	}
	return (nodes);
}
